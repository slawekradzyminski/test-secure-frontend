/// <reference types="cypress" />

describe('Iframe', () => {
    beforeEach(() => {
        cy.visit('./iframe.html')
    })

    it('should successfully register & the login', {baseUrl: null}, () => {
        
    })

})
